apiVersion: v1
kind: Service
metadata:
  name: {{ include "glossary.fullname" . }}
  labels:
    {{- include "glossary.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "glossary.selectorLabels" . | nindent 4 }}
