apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "glossary.fullname" . }}-test-connection"
  labels:
    {{- include "glossary.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "glossary.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
